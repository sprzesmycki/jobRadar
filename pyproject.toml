[project]
name = "przeprogramowani"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.5",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
]
